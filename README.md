# List of Fields

```
proposal
        proposal.title
        proposal.description
        proposal.id
        proposal.createdate

freelancer
        freelancer.id
        freelancer.wallet
        freelancer.name
        freelancer.address
                address.street
                address.city
                address.country

client
        client.id
        client.wallet
        client.name
        client.address
                address.street
                address.city
                address.country
milestone
        milestone.price
        milestone.description
        milestone.deadline
        milestone.status
        milestone.createdate
        milestone.id
        milestone.extended
```
